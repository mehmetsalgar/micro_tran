spring:
  application:
    name: product_process_${project.version}
  jmx:
    default-domain: product_process_${project.version}
  cloud:
    config:
      uri: ${vcap.services.${PREFIX:}configserver.credentials.uri:http://user:password@localhost:8888}